document.querySelectorAll(".input").forEach(e => e.classList.add("notranslate"));
document.querySelectorAll(".output_wrapper").forEach(e => e.classList.add("notranslate"));
// スクロールできるようにする
document.querySelectorAll("body").forEach(e => e.style.overflowY="visible");
